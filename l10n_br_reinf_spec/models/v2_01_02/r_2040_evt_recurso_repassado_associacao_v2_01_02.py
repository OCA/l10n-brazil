# Copyright 2025 Akretion - Raphaël Valyi <raphael.valyi@akretion.com>
# License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl-3.0.en.html).
# Generated by https://github.com/akretion/xsdata-odoo

from __future__ import annotations

import textwrap

from odoo import fields, models

__NAMESPACE__ = (
    "http://www.reinf.esocial.gov.br/schemas/evtRecursoRepassadoAssociacao/v2_01_02"
)


class Reinf(models.AbstractModel):
    """Escrituracao Fiscal Digital de Retencoes e Outras Informacoes Fiscais
    (EFD-Reinf)"""

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.reinf"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf"

    reinf21_evtAssocDespRep = fields.Many2one(
        comodel_name="reinf.21.evtassocdesprep",
        string="Evento recursos repassados",
        xsd_required=True,
        help="Evento recursos repassados para associacoes desportivas",
    )


class EvtAssocDespRep(models.AbstractModel):
    "Evento recursos repassados para associacoes desportivas"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtassocdesprep"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep"

    reinf21_ideEvento = fields.Many2one(
        comodel_name="reinf.21.evtassocdesprep_ideevento",
        string="Informacoes de identificacao do evento",
        xsd_required=True,
    )

    reinf21_ideContri = fields.Many2one(
        comodel_name="reinf.21.evtassocdesprep_idecontri",
        string="Informacoes de identificacao",
        xsd_required=True,
        help="Informacoes de identificacao do contribuinte",
    )

    reinf21_id = fields.Char(string="Identificacao unica do evento", xsd_required=True)


class EvtAssocDespRepIdeEvento(models.AbstractModel):
    "Informacoes de identificacao do evento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtassocdesprep_ideevento"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep.IdeEvento"

    reinf21_indRetif = fields.Char(
        string="Indicativo de Retificacao", xsd_required=True
    )

    reinf21_nrRecibo = fields.Char(string="Recibo arquivo a ser retificado")

    reinf21_perApur = fields.Char(string="Periodo de apuracao", xsd_required=True)

    reinf21_tpAmb = fields.Char(string="Tipo de ambiente", xsd_required=True)

    reinf21_procEmi = fields.Char(
        string="Processo de emissao do evento", xsd_required=True
    )

    reinf21_verProc = fields.Char(
        string="Versao do processo de emissao do evento", xsd_required=True
    )


class EvtAssocDespRepIdeContri(models.AbstractModel):
    "Informacoes de identificacao do contribuinte"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtassocdesprep_idecontri"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep.IdeContri"

    reinf21_tpInsc = fields.Char(string="Tipo de Inscricao", xsd_required=True)

    reinf21_nrInsc = fields.Char(string="Numero de Inscricao", xsd_required=True)

    reinf21_ideEstab = fields.Many2one(
        comodel_name="reinf.21.evtassocdesprep_ideestab",
        string="Identificacao do estabelecimento",
        xsd_required=True,
        help="Identificacao do estabelecimento que repassou recursos",
    )


class EvtAssocDespRepIdeEstab(models.AbstractModel):
    "Identificacao do estabelecimento que repassou recursos"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtassocdesprep_ideestab"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep.IdeContri.IdeEstab"

    reinf21_tpInscEstab = fields.Char(
        string="Tipo de inscricao do estabelecimento", xsd_required=True
    )

    reinf21_nrInscEstab = fields.Char(
        string="Numero de inscricao do estabelecimento", xsd_required=True
    )

    reinf21_recursosRep = fields.One2many(
        "reinf.21.recursosrep",
        "reinf21_recursosRep_ideEstab_id",
        string="Recursos repassados",
        help="Recursos repassados para associacoes desportivas",
    )


class RecursosRep(models.AbstractModel):
    "Recursos repassados para associacoes desportivas"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.recursosrep"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep.IdeContri.IdeEstab.RecursosRep"

    reinf21_recursosRep_ideEstab_id = fields.Many2one(
        comodel_name="reinf.21.evtassocdesprep_ideestab",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_cnpjAssocDesp = fields.Char(
        string="CNPJ da Associacao Desportiva", xsd_required=True
    )

    reinf21_vlrTotalRep = fields.Char(string="Valor Total Repassado", xsd_required=True)

    reinf21_vlrTotalRet = fields.Char(
        string="Soma do valor da retencao",
        xsd_required=True,
        help="Soma do valor da retencao dos recursos recebidos",
    )

    reinf21_vlrTotalNRet = fields.Char(
        string="Valor da retencao nao efetuada",
        help=(
            "Valor da retencao nao efetuada ou depositada em juizo em "
            "decorrencia da decisao judicial/administrativa"
        ),
    )

    reinf21_infoRecurso = fields.One2many(
        "reinf.21.evtassocdesprep_inforecurso",
        "reinf21_infoRecurso_recursosRep_id",
        string="Detalhamento dos recursos repassados",
    )

    reinf21_infoProc = fields.One2many(
        "reinf.21.evtassocdesprep_infoproc",
        "reinf21_infoProc_recursosRep_id",
        string="Informacoes",
        help="Informacoes de processos administrativos/judiciais",
    )


class EvtAssocDespRepInfoRecurso(models.AbstractModel):
    "Detalhamento dos recursos repassados"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtassocdesprep_inforecurso"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep.IdeContri.IdeEstab.RecursosRep.InfoRecurso"

    reinf21_infoRecurso_recursosRep_id = fields.Many2one(
        comodel_name="reinf.21.recursosrep",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_tpRepasse = fields.Char(string="Tipo de Repasse", xsd_required=True)

    reinf21_descRecurso = fields.Char(
        string="Descricao resumida do recurso repassado", xsd_required=True
    )

    reinf21_vlrBruto = fields.Char(string="Valor bruto", xsd_required=True)

    reinf21_vlrRetApur = fields.Char(
        string="Valor da apuracao da retencao",
        xsd_required=True,
        help=("Valor da apuracao da retencao que deve sofrer a associacao desportiva"),
    )


class EvtAssocDespRepInfoProc(models.AbstractModel):
    "Informacoes de processos administrativos/judiciais"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtassocdesprep_infoproc"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAssocDespRep.IdeContri.IdeEstab.RecursosRep.InfoProc"

    reinf21_infoProc_recursosRep_id = fields.Many2one(
        comodel_name="reinf.21.recursosrep",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_tpProc = fields.Char(string="Tipo de Processo", xsd_required=True)

    reinf21_nrProc = fields.Char(string="Numero do Processo", xsd_required=True)

    reinf21_codSusp = fields.Char(string="Codigo do indicativo da suspensao")

    reinf21_vlrNRet = fields.Char(
        string="Valor da retencao que deixou",
        xsd_required=True,
        help="Valor da retencao que deixou de ser efetuada",
    )
