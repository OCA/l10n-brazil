# Copyright 2025 Akretion - Raphaël Valyi <raphael.valyi@akretion.com>
# License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl-3.0.en.html).
# Generated by https://github.com/akretion/xsdata-odoo

from __future__ import annotations

import textwrap

from odoo import fields, models

__NAMESPACE__ = (
    "http://www.reinf.esocial.gov.br/schemas/evt2055AquisicaoProdRural/v2_01_02"
)


class Reinf(models.AbstractModel):
    """Escrituracao Fiscal Digital de Retencoes e Outras Informacoes Fiscais
    (EFD-Reinf)"""

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.reinf"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf"

    reinf21_evtAqProd = fields.Many2one(
        comodel_name="reinf.21.evtaqprod",
        string="Evento aquisicao de producao",
        xsd_required=True,
    )


class EvtAqProd(models.AbstractModel):
    "Evento aquisicao de producao"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtaqprod"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd"

    reinf21_ideEvento = fields.Many2one(
        comodel_name="reinf.21.evtaqprod_ideevento",
        string="Informacoes de identificacao do evento",
        xsd_required=True,
    )

    reinf21_ideContri = fields.Many2one(
        comodel_name="reinf.21.evtaqprod_idecontri",
        string="Informacoes de identificacao",
        xsd_required=True,
        help="Informacoes de identificacao do contribuinte",
    )

    reinf21_infoAquisProd = fields.Many2one(
        comodel_name="reinf.21.infoaquisprod",
        string="Informacoes da aquisicao de producao",
        xsd_required=True,
    )

    reinf21_id = fields.Char(string="Identificacao unica do evento", xsd_required=True)


class EvtAqProdIdeEvento(models.AbstractModel):
    "Informacoes de identificacao do evento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtaqprod_ideevento"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd.IdeEvento"

    reinf21_indRetif = fields.Char(
        string="Indicativo de Retificacao", xsd_required=True
    )

    reinf21_nrRecibo = fields.Char(string="Recibo arquivo a ser retificado")

    reinf21_perApur = fields.Char(string="Periodo de apuracao", xsd_required=True)

    reinf21_tpAmb = fields.Char(string="Tipo de ambiente", xsd_required=True)

    reinf21_procEmi = fields.Char(
        string="Processo de emissao do evento", xsd_required=True
    )

    reinf21_verProc = fields.Char(
        string="Versao do processo de emissao do evento", xsd_required=True
    )


class EvtAqProdIdeContri(models.AbstractModel):
    "Informacoes de identificacao do contribuinte"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtaqprod_idecontri"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd.IdeContri"

    reinf21_tpInsc = fields.Char(string="Tipo de Inscricao", xsd_required=True)

    reinf21_nrInsc = fields.Char(string="Numero de Inscricao", xsd_required=True)


class InfoAquisProd(models.AbstractModel):
    "Informacoes da aquisicao de producao"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.infoaquisprod"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd.InfoAquisProd"

    reinf21_ideEstabAdquir = fields.Many2one(
        comodel_name="reinf.21.ideestabadquir",
        string="Identificacao",
        xsd_required=True,
        help="Identificacao do estabelecimento adquirente da producao",
    )


class IdeEstabAdquir(models.AbstractModel):
    "Identificacao do estabelecimento adquirente da producao"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.ideestabadquir"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd.InfoAquisProd.IdeEstabAdquir"

    reinf21_tpInscAdq = fields.Char(
        string="Tipo de inscricao do adquirente", xsd_required=True
    )

    reinf21_nrInscAdq = fields.Char(
        string="Indicar o numero de inscricao", xsd_required=True
    )

    reinf21_ideProdutor = fields.Many2one(
        comodel_name="reinf.21.ideprodutor",
        string="Identificacao do produtor rural",
        xsd_required=True,
    )


class IdeProdutor(models.AbstractModel):
    "Identificacao do produtor rural"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.ideprodutor"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd.InfoAquisProd.IdeEstabAdquir.IdeProdutor"

    reinf21_tpInscProd = fields.Char(
        string="Tipo de inscricao do produtor", xsd_required=True
    )

    reinf21_nrInscProd = fields.Char(string="Inscricao do produtor", xsd_required=True)

    reinf21_indOpcCP = fields.Char(string="Indicativo de opcao da CP")

    reinf21_detAquis = fields.One2many(
        "reinf.21.detaquis",
        "reinf21_detAquis_ideProdutor_id",
        string="Detalhamento da aquisicao",
        help="Detalhamento da aquisicao de producao rural",
    )


class DetAquis(models.AbstractModel):
    "Detalhamento da aquisicao de producao rural"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.detaquis"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtAqProd.InfoAquisProd.IdeEstabAdquir.IdeProdutor.DetAquis"

    reinf21_detAquis_ideProdutor_id = fields.Many2one(
        comodel_name="reinf.21.ideprodutor",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_indAquis = fields.Char(string="Indicativo da Aquisicao", xsd_required=True)

    reinf21_vlrBruto = fields.Char(string="Valor bruto", xsd_required=True)

    reinf21_vlrCPDescPR = fields.Char(
        string="Vr da Contrib. Previd",
        xsd_required=True,
        help="Vr da Contrib. Previd. descontada pelo adquirente",
    )

    reinf21_vlrRatDescPR = fields.Char(
        string="Valor da GILRAT incidente sobre",
        xsd_required=True,
        help="Valor da GILRAT incidente sobre a aquisicao de producao",
    )

    reinf21_vlrSenarDesc = fields.Char(
        string="Valor da contribuicao destinada ao Senar", xsd_required=True
    )

    reinf21_infoProcJud = fields.One2many(
        "reinf.21.evtaqprod_infoprocjud",
        "reinf21_infoProcJud_detAquis_id",
        string="Informacao de processos judiciais",
        help="Informacao de processos judiciais do produtor rural",
    )


class EvtAqProdInfoProcJud(models.AbstractModel):
    "Informacao de processos judiciais do produtor rural"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtaqprod_infoprocjud"
    _inherit = "spec.mixin.reinf"
    _binding_type = (
        "Reinf.EvtAqProd.InfoAquisProd.IdeEstabAdquir.IdeProdutor.DetAquis.InfoProcJud"
    )

    reinf21_infoProcJud_detAquis_id = fields.Many2one(
        comodel_name="reinf.21.detaquis",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_nrProcJud = fields.Char(string="Numero do Processo", xsd_required=True)

    reinf21_codSusp = fields.Char(string="Codigo do indicativo da suspensao")

    reinf21_vlrCPNRet = fields.Char(
        string="Valor da Contrib. Previd",
        help=("Valor da Contrib. Previd. nao retida em decorrencia de processo"),
    )

    reinf21_vlrRatNRet = fields.Char(string="Vr RAT nao retido")

    reinf21_vlrSenarNRet = fields.Char(string="Vr Senar nao retido")
