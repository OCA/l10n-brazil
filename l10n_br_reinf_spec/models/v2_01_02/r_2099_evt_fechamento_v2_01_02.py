# Copyright 2025 Akretion - Raphaël Valyi <raphael.valyi@akretion.com>
# License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl-3.0.en.html).
# Generated by https://github.com/akretion/xsdata-odoo

from __future__ import annotations

import textwrap

from odoo import fields, models

__NAMESPACE__ = "http://www.reinf.esocial.gov.br/schemas/evtFechamento/v2_01_02"


class Reinf(models.AbstractModel):
    """Escrituracao Fiscal Digital de Retencoes e Outras Informacoes Fiscais
    (EFD-Reinf)"""

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.reinf"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf"

    reinf21_evtFechaEvPer = fields.Many2one(
        comodel_name="reinf.21.evtfechaevper",
        string="Evento de fechamento",
        xsd_required=True,
    )


class EvtFechaEvPer(models.AbstractModel):
    "Evento de fechamento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtfechaevper"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtFechaEvPer"

    reinf21_ideEvento = fields.Many2one(
        comodel_name="reinf.21.evtfechaevper_ideevento",
        string="Informacoes de identificacao do evento",
        xsd_required=True,
    )

    reinf21_ideContri = fields.Many2one(
        comodel_name="reinf.21.evtfechaevper_idecontri",
        string="Informacoes de identificacao",
        xsd_required=True,
        help="Informacoes de identificacao do contribuinte",
    )

    reinf21_ideRespInf = fields.Many2one(
        comodel_name="reinf.21.evtfechaevper_iderespinf",
        string="Responsavel pelas informacoes",
    )

    reinf21_infoFech = fields.Many2one(
        comodel_name="reinf.21.evtfechaevper_infofech",
        string="Informacoes do fechamento",
        xsd_required=True,
    )

    reinf21_id = fields.Char(string="Identificacao unica do evento", xsd_required=True)


class EvtFechaEvPerIdeEvento(models.AbstractModel):
    "Informacoes de identificacao do evento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtfechaevper_ideevento"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtFechaEvPer.IdeEvento"

    reinf21_perApur = fields.Char(string="Periodo de apuracao", xsd_required=True)

    reinf21_tpAmb = fields.Char(string="Tipo de ambiente", xsd_required=True)

    reinf21_procEmi = fields.Char(
        string="Processo de emissao do evento", xsd_required=True
    )

    reinf21_verProc = fields.Char(
        string="Versao do processo de emissao do evento", xsd_required=True
    )


class EvtFechaEvPerIdeContri(models.AbstractModel):
    "Informacoes de identificacao do contribuinte"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtfechaevper_idecontri"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtFechaEvPer.IdeContri"

    reinf21_tpInsc = fields.Char(string="Tipo de Inscricao", xsd_required=True)

    reinf21_nrInsc = fields.Char(string="Numero de Inscricao", xsd_required=True)


class EvtFechaEvPerIdeRespInf(models.AbstractModel):
    "Responsavel pelas informacoes"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtfechaevper_iderespinf"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtFechaEvPer.IdeRespInf"

    reinf21_nmResp = fields.Char(
        string="Responsavel pelas informacoes", xsd_required=True
    )

    reinf21_cpfResp = fields.Char(
        string="Numero de Inscricao no CPF", xsd_required=True
    )

    reinf21_telefone = fields.Char(string="Numero do telefone com DDD")

    reinf21_email = fields.Char(string="Endereco eletronico")


class EvtFechaEvPerInfoFech(models.AbstractModel):
    "Informacoes do fechamento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtfechaevper_infofech"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtFechaEvPer.InfoFech"

    reinf21_evtServTm = fields.Char(
        string="Servicos tomados no periodo de apuracao", xsd_required=True
    )

    reinf21_evtServPr = fields.Char(
        string="Servicos Prestados no periodo",
        xsd_required=True,
        help="Servicos Prestados no periodo de apuracao",
    )

    reinf21_evtAssDespRec = fields.Char(
        string="Evento recebimento de recursos",
        xsd_required=True,
        help="Evento recebimento de recursos por associacao desportiva",
    )

    reinf21_evtAssDespRep = fields.Char(
        string="Evento repasse de recursos",
        xsd_required=True,
        help="Evento repasse de recursos a associacao desportiva",
    )

    reinf21_evtComProd = fields.Char(
        string="Informacoes de comercializacao",
        xsd_required=True,
        help="Informacoes de comercializacao da Producao rural",
    )

    reinf21_evtCPRB = fields.Char(string="Informacoes da CPRB", xsd_required=True)

    reinf21_evtAquis = fields.Char(
        string="Evento aquisicao de producao rural", xsd_required=True
    )
