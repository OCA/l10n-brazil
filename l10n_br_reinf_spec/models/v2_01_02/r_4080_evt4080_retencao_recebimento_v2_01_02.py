# Copyright 2025 Akretion - Raphaël Valyi <raphael.valyi@akretion.com>
# License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl-3.0.en.html).
# Generated by https://github.com/akretion/xsdata-odoo

from __future__ import annotations

import textwrap

from odoo import fields, models

__NAMESPACE__ = (
    "http://www.reinf.esocial.gov.br/schemas/evt4080RetencaoRecebimento/v2_01_02"
)


class Reinf(models.AbstractModel):
    """Escrituracao Fiscal Digital de Retencoes e Outras Informacoes Fiscais
    (EFD-Reinf)"""

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.reinf"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf"

    reinf21_evtRetRec = fields.Many2one(
        comodel_name="reinf.21.evtretrec",
        string="Evento Retencao no Recebimento",
        xsd_required=True,
    )


class EvtRetRec(models.AbstractModel):
    "Evento Retencao no Recebimento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtretrec"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec"

    reinf21_ideEvento = fields.Many2one(
        comodel_name="reinf.21.evtretrec_ideevento",
        string="Informacoes de identificacao do evento",
        xsd_required=True,
    )

    reinf21_ideContri = fields.Many2one(
        comodel_name="reinf.21.evtretrec_idecontri",
        string="Informacoes de identificacao",
        xsd_required=True,
        help="Informacoes de identificacao do contribuinte",
    )

    reinf21_ideEstab = fields.Many2one(
        comodel_name="reinf.21.evtretrec_ideestab",
        string="Identificacao do estabelecimento",
        xsd_required=True,
    )

    reinf21_id = fields.Char(string="Identificacao unica do evento", xsd_required=True)


class EvtRetRecIdeEvento(models.AbstractModel):
    "Informacoes de identificacao do evento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtretrec_ideevento"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeEvento"

    reinf21_indRetif = fields.Char(
        string="Indicativo de Retificacao", xsd_required=True
    )

    reinf21_nrRecibo = fields.Char(string="Recibo arquivo a ser retificado")

    reinf21_perApur = fields.Char(string="Periodo de apuracao", xsd_required=True)

    reinf21_tpAmb = fields.Char(string="Tipo de ambiente", xsd_required=True)

    reinf21_procEmi = fields.Char(
        string="Processo de emissao do evento", xsd_required=True
    )

    reinf21_verProc = fields.Char(
        string="Versao do processo de emissao do evento", xsd_required=True
    )


class EvtRetRecIdeContri(models.AbstractModel):
    "Informacoes de identificacao do contribuinte"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtretrec_idecontri"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeContri"

    reinf21_tpInsc = fields.Char(string="Tipo de Inscricao", xsd_required=True)

    reinf21_nrInsc = fields.Char(string="Numero de Inscricao", xsd_required=True)


class EvtRetRecIdeEstab(models.AbstractModel):
    "Identificacao do estabelecimento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtretrec_ideestab"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeEstab"

    reinf21_tpInscEstab = fields.Char(
        string="Tipo de inscricao do estabelecimento", xsd_required=True
    )

    reinf21_nrInscEstab = fields.Char(
        string="Numero de inscricao do estabelecimento", xsd_required=True
    )

    reinf21_ideFont = fields.Many2one(
        comodel_name="reinf.21.idefont",
        string="Identificacao da fonte pagadora",
        xsd_required=True,
    )


class IdeFont(models.AbstractModel):
    "Identificacao da fonte pagadora"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.idefont"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeEstab.IdeFont"

    reinf21_cnpjFont = fields.Char(
        string="CNPJ da fonte pagadora do rendimento", xsd_required=True
    )

    reinf21_ideRend = fields.One2many(
        "reinf.21.iderend",
        "reinf21_ideRend_ideFont_id",
        string="Identificacao do rendimento",
    )


class IdeRend(models.AbstractModel):
    "Identificacao do rendimento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.iderend"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeEstab.IdeFont.IdeRend"

    reinf21_ideRend_ideFont_id = fields.Many2one(
        comodel_name="reinf.21.idefont",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_natRend = fields.Char(string="Natureza do Rendimento", xsd_required=True)

    reinf21_observ = fields.Char(string="Observacoes")

    reinf21_infoRec = fields.One2many(
        "reinf.21.inforec",
        "reinf21_infoRec_ideRend_id",
        string="Informacoes relativas ao recebimento",
        help="Informacoes relativas ao recebimento do rendimento",
    )


class InfoRec(models.AbstractModel):
    "Informacoes relativas ao recebimento do rendimento"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.inforec"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeEstab.IdeFont.IdeRend.InfoRec"

    reinf21_infoRec_ideRend_id = fields.Many2one(
        comodel_name="reinf.21.iderend",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_dtFG = fields.Char(string="Data do pagamento", xsd_required=True)

    reinf21_vlrBruto = fields.Char(string="Valor bruto", xsd_required=True)

    reinf21_vlrBaseIR = fields.Char(string="Valor da base do IR", xsd_required=True)

    reinf21_vlrIR = fields.Char(string="Valor da retencao do IR", xsd_required=True)

    reinf21_observ = fields.Char(string="Observacoes")

    reinf21_infoProcRet = fields.One2many(
        "reinf.21.evtretrec_infoprocret",
        "reinf21_infoProcRet_infoRec_id",
        string="Processos relacionados a nao retencao",
        help="Processos relacionados a nao retencao de tributos",
    )


class EvtRetRecInfoProcRet(models.AbstractModel):
    "Processos relacionados a nao retencao de tributos"

    _description = textwrap.dedent(f"    {__doc__}")
    _name = "reinf.21.evtretrec_infoprocret"
    _inherit = "spec.mixin.reinf"
    _binding_type = "Reinf.EvtRetRec.IdeEstab.IdeFont.IdeRend.InfoRec.InfoProcRet"

    reinf21_infoProcRet_infoRec_id = fields.Many2one(
        comodel_name="reinf.21.inforec",
        xsd_implicit=True,
        ondelete="cascade",
        index=True,
    )
    reinf21_tpProcRet = fields.Char(string="Tipo de Processo", xsd_required=True)

    reinf21_nrProcRet = fields.Char(string="Numero do Processo", xsd_required=True)

    reinf21_codSusp = fields.Char(string="Codigo do indicativo da suspensao")

    reinf21_vlrBaseSuspIR = fields.Char(
        string="Valor da base do IR",
        help="Valor da base do IR com exigibilidade suspensa",
    )

    reinf21_vlrNIR = fields.Char(string="Valor nao retido de IR")

    reinf21_vlrDepIR = fields.Char(string="Valor do deposito judicial de IR")
