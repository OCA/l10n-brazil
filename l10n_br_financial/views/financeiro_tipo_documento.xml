<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2017 KMEE
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->

<odoo>
<data>

    <record model="ir.ui.view" id="financeiro_tipo_documento_form_view">
        <field name="name">financeiro.tipo_documento.form (in l10n_br_financial)</field>
        <field name="model">financeiro.tipo_documento</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <!-- TODO -->
                </header>
                <sheet>
                    <!-- TODO -->
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
                <div class="oe_chatter"></div>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="financeiro_tipo_documento_search_view">
        <field name="name">financeiro.tipo_documento.search (in l10n_br_financial)</field>
        <field name="model">financeiro.tipo_documento</field>
        <field name="arch" type="xml">
            <search>
                <!-- TODO -->
            </search>
        </field>
    </record>

    <record model="ir.ui.view" id="financeiro_tipo_documento_tree_view">
        <field name="name">financeiro.tipo_documento.tree (in l10n_br_financial)</field>
        <field name="model">financeiro.tipo_documento</field>
        <field name="arch" type="xml">
            <tree>
                <!-- TODO -->
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="financeiro_tipo_documento_act_window">
        <field name="name">Tipo Documento Financeiro</field> <!-- TODO -->
        <field name="res_model">financeiro.tipo_documento</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record model="ir.ui.menu" id="financeiro_tipo_documento_menu">
        <field name="name">Tipo Documento</field>
        <field name="action" ref="financeiro_tipo_documento_act_window"/>
        <field name="sequence" eval="16"/>
        <field name="parent_id" ref="financial.financial_config_menu"/>
    </record>

</data>
</odoo>
