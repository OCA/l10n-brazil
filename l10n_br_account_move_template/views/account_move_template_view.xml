<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

    <record id="account_move_template_tree_view" model="ir.ui.view">
        <field name="name">account.move.template.tree.view</field>
        <field name="model">account.move.template</field>
        <field name="arch" type="xml">
            <tree string="Move Template" editable="top">   <!--decoration-info="state == 'draft'" decoration-muted="state == 'cancel'"-->
                <field name="company_id"/>
                <field name="fiscal_category_ids"/>
            </tree>
        </field>
    </record>

    <record id="account_move_template_form_view" model="ir.ui.view">
        <field name="name">account.move.template.form.view</field>
        <field name="model">account.move.template</field>
        <field name="arch" type="xml">
            <form><!--decoration-info="state == 'draft'" decoration-muted="state == 'cancel'"-->
                <group>
                    <field name="company_id"/>
                    <field name="fiscal_category_ids"/>
                </group>
            </form>
        </field>
    </record>


    <record id="action_account_move_template" model="ir.actions.act_window">
            <field name="name">Move Templates</field>
            <field name="res_model">account.move.template</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="account_move_template_tree_view"/>
            <field name="context">{}</field>
            <!--<field name="search_view_id" ref="view_account_invoice_filter"/>-->
        </record>

    <menuitem action="action_account_move_template"
              id="menu_action_account_move_template" parent="account.account_account_menu" sequence="90"/>
    </data>
</openerp>
