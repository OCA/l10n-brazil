<odoo>
    <template id="pix_inter_redirect_template">
        <form id="o_payment_redirect_form" t-att-action="redirect_url" method="get">
            <input type="hidden" name="reference" t-att-value="reference"/>
        </form>
    </template>

    <template id="pix_inter_page_template">
        <div style="display:flex; justify-content:center; align-items:center; height:100vh; background-color:#f7f7f7; font-family:Arial, sans-serif;">
            <div style="background:#ffffff; padding:30px; border-radius:15px; box-shadow:0 4px 15px rgba(0,0,0,0.1); max-width:400px; width:100%; text-align:center;">
                <h3 style="margin-bottom:20px; color:#333;">Pagamento PIX</h3>
                <p style="margin-bottom:10px; font-size:1.1em;">Valor: <strong>R$ <span t-esc="'%.2f' % amount"/></strong></p>
                <p style="margin-bottom:20px; font-size:0.95em; color:#555;">Referência: <strong><span t-esc="reference"/></strong></p>
                <div style="margin-bottom:20px;">
                    <img t-att-src="'data:image/png;base64,%s' % qrcode_base64" alt="QR Code PIX" style="max-width:250px; width:100%; border:2px solid #eee; border-radius:10px;"/>
                </div>
                <div style="margin-bottom:0;">
                    <label for="pix_code" style="display:block; margin-bottom:5px; font-weight:bold; color:#333;">Código PIX (Copiar e Colar):</label>
                    <div style="display:flex; width:100%;">
                        <input type="text" id="pix_code" t-att-value="pix_code" readonly="readonly"
                               style="flex:1; padding:10px; border-radius:8px 0 0 8px; border:1px solid #ccc; font-size:0.95em;"/>
                        <button style="padding:10px 15px; background-color:#007bff; color:#fff; border:none; border-radius:0 8px 8px 0; cursor:pointer; font-weight:bold;">
                            Copiar
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </template>
</odoo>
