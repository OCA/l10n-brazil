Create a service fiscal document (model "SE") for a company configured for the
national environment, confirm it and send it. The module builds and signs the
DPS, posts it to the ADN and, on authorization, stores the NFS-e XML, its
50-digit access key and protocol, and renders the DANFSe.
